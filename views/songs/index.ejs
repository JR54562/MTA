<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title></title>
    <link rel="stylesheet" href="/css/app.css" />
  </head>
  <body>
    <h1>Song index page</h1>

    <ul>
      <% song.forEach ((s, index) => { %>
      <li>
        <span id="titleText">Song Title: <%= s.name %></span> <br/>
Collection: <%=s.album%>; Year: <%=s.year%>; Length: <%=s.length%>; Genre: <%=s.genre%><br>
        <form action="/songs/<%= s.id %>?_method=DELETE" method="POST">
          <input class="smDel" type="submit" value="DELETE" />&nbsp;&nbsp;&nbsp;
          <a class="smEdit" href="/songs/<%=s.id; %>/edit">Edit</a>
        </form>
        <br />
      </li>
      <% }) %>
    </ul>
    <nav>
      <a href="/songs/new">Create a New Song</a><br />
      <a href="/users">Visit Your Profile</a>
    </nav>
  </body>
</html>
